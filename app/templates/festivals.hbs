{{app-nav}}

<div class="container">
	<div class="row">
		<div class="col-md-2">
		{{#link-to 'festivals.create'}}
			<button class="btn btn-success"> Créer un festival </button>
		{{/link-to}}
		</div>
	</div>
</div>

{{festival-list title="Liste des Festivals" festivals = model}}
{{outlet}}